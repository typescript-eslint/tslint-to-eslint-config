import { RuleConverter } from "../converter";

export const convertRadix: RuleConverter = () => {
    return {
        rules: [
            {
                ruleName: "radix",
            },
        ],
    };
};
